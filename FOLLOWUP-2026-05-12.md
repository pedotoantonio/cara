# Follow-up Cara — 2026-05-12

Sessione di correzioni con Nyx. Items chiusi e items da fare.

## ✅ Applicato oggi (tutto reversibile)

### Server (NanoPC-T6)
- **Swap 2GB** (`/swapfile`, `vm.swappiness=10`)
- **6 update security** applicati (lcms2, libpng16, linux-libc-dev, openjdk-17, tzdata)
- **snmpd** ripristinato dopo crash settimana scorsa
- **Bluetooth SAP plugin** disabilitato (override systemd `--noplugin=sap`)

### Cara
- **pgvector 0.8.0**: immagine custom `cara-postgres-pgvector:16-0.8.0` (`/opt/cara/postgres-pgvector/Dockerfile`). Postgres recreate con stessa UID 70, volume `./data/postgres` compatibile.
- **Runtime rkllm v1.1.1**: `/home/apedo/cara-poc-rkllm/runtime-v1.1.1/librkllmrt.so`. v1.1.0 tenuto come fallback. Compose aggiornato.
- **System prompt ridotto -60%**: salvato in `admin_settings.llm_system_prompt`. TTFT 17s → 7.6s. Rollback: `UPDATE admin_settings SET value='null' WHERE key='llm_system_prompt';`
- **KV cache persistente**: bind-mount `./data/kv_cache:/app/cache/kv` aggiunto.
- **Chroma rimosso**: codice morto, container stop+rm, riferimenti Python eliminati. Backend image ricompilata e deployata, zero riferimenti Chroma rimasti nel runtime.
- **Backup Postgres con mirror MinIO**: script `/opt/cara/scripts/backup-postgres.sh` esteso. Doppia copia: locale `/opt/cara/backups/` + bucket MinIO `cara-backups`. Cron 03:15 già attivo. Retention 7gg su entrambi.
- **Observability LLM minima**: script `/opt/cara/scripts/llm-stats.sh [window]` aggrega TTFT/tok_per_s/silent_failures/load times dai log strutturati. Esempio: `llm-stats.sh 24h`.

### Tentativo fallito — documentato
- **Qwen 3B w8a8**: caricava con runtime 1.1.0 ma emetteva 0 token (silent failure). Con runtime 1.1.1 emette 12 token in 30s ma degradato (TTFT 28s, output incoerente). Ipotesi: 3.74GB modello + KV cache > cap NPU pratico ~3GB. Modello lasciato in `/opt/cara/data/models/qwen2.5-3b-instruct-w8a8.rkllm` (3.74GB) — eliminabile.

## 🟡 Da fare (in ordine di impatto)

### 1. RAG ingestion su pgvector — IL CAMBIO PIÙ IMPATTANTE
**Perché:** la "stupidità" di Cara è assenza di knowledge base ingerita, non bug. 2 fact + 3 note in 13 giorni. Senza retrieval, anche un 7B resta ignorante sui fatti di casa.

**Step concreti:**
1. Alembic migration: aggiungere colonna `vector(384)` a `facts` (oggi JSONB), nuove tabelle `note_embeddings`, `conversation_summaries_embeddings`. Indici HNSW: `CREATE INDEX ON facts USING hnsw (embedding vector_cosine_ops);`
2. Aggiornare `cara/models/fact.py` da `JSONB` a `pgvector.sqlalchemy.Vector(384)`. Dipendenza: `pip install pgvector`.
3. Nuovo celery task `cara/agents/learn.py::ingest_recent` (cron giornaliero): per ogni nuovo `Note`, `CalendarEvent`, `Message` salient → calcola embedding → upsert.
4. Hook nel `_chat_pipeline`: stage RAG PRIMA del LLM. Top-3 similarity sulla query utente → inietta nel prompt come `# Contesto:` block.
5. Trigger inizio: backfill `facts` e `notes` esistenti.

**Stima:** 4-6 ore di codice + test. Migration data trivial (2 facts).

### 2. Popolare dispatcher tier2 (fast-path no-LLM)
**Perché:** oggi `n_skills=1`. Saluti, conferme, smart home base non dovrebbero toccare l'LLM. Risparmio: ~80% delle chiamate LLM eliminate, libera NPU per query vere.

**Step:**
1. Skill manuali: `greeting`, `thanks`, `agreement`, `time_query`, `who_is_home_fast` (wrapper deterministico).
2. Verificare perché `learn.reflective_run` non genera nuove proposte (cluster threshold?). Probabilmente la famiglia genera poche miss, ma quelle che genera sono troppo eterogenee per cluster.
3. Considerare abbassare la soglia di Skill Author per testare il flusso.

### 3. ~~Rebuild backend image~~ — FATTO (immagine ricompilata + deployata 18:00 UTC).

### 4. Frigate HW decode (rimandato)
**Perché rimandato:** richiede switch immagine da `ghcr.io/blakeblackshear/frigate:0.14.1` a `:stable-rockchip` o `:0.15-rockchip`. Cambio invasivo su 4 camere senza staging, finestra dedicata + rollback plan.

**Quando:** quando hai 30 min e accetti 1 minuto di camere offline.

### 5. Profili familiari + identità nella chat — BLOCCATO, serve input da Antonio
**Perché bloccato:** la tabella `users` ha 35 utenti attivi di cui **34 sono `cara-test-*@example.com` (junk)** e solo 1 reale (Antonio Pedoto). La famiglia non è registrata. Senza dati famiglia veri, l'injection di `user.full_name` nel system prompt non porta valore.

**Step a tuo carico (Antonio):**
1. Decidere se cleanup dei 34 test users è ok (sembrano residuo QA):
   ```sql
   DELETE FROM users WHERE email LIKE 'cara-test-%';
   ```
2. Inserire i veri membri famiglia: Sara, Matteo, Ilaria (vedo i nomi da `presence_events`). Ognuno con `full_name`, `birth_date`, `role` (parent/child).
3. Decidere quali profili stabili vuoi: dietary, scuola, lavoro, allergie, preferenze → finiscono nella tabella `facts` con `subject=full_name`.

**Step a mio carico una volta che ci sono dati veri:**
1. Patch `_chat_pipeline.py` per iniettare `# Stai parlando con {user.full_name} ({user.role})` all'inizio del prompt quando JWT user ≠ Antonio. Trivial: 5 righe + rebuild.
2. Wiring presence proactivity → "Matteo è arrivato a casa" come Telegram alert (la struttura `presence_events` c'è già).
3. RAG su `facts WHERE subject=user.full_name`: top-3 facts iniettati come `# Su {nome}:` block.

### 6. Conversation summarization — work pending
**Perché:** conversazioni lunghe accumulano prompt → TTFT cresce. Dopo N turni vale comprimere "stato conversazione" in 50 token.

**Step:**
1. Migration: aggiungere colonna `summary text` a `conversations`.
2. Celery task `cara/agents/learn.py::summarize_stale` (cron orario): per conversation con `n_messages > 10` e `summary IS NULL OR updated_at < last_message_time - 1h`, chiamata LLM con prompt "Riassumi questa conversazione in 50 parole" → save.
3. Modificare `_chat_pipeline.build_prompt` per usare `summary + last 3 messages` invece di full history quando `summary IS NOT NULL`.

**Stima:** 2-3 ore + test.

### 7. Investigazione 3B (se vuoi riprovarci)
Provare:
- Variante `opt-0-hybrid-ratio-0.0` (più conservativa, scaricabile da `c01zaut/Qwen2.5-3B-Instruct-rk3588-1.1.1`)
- Modello 3B w4 (quantizzazione più aggressiva, ~2GB invece di 3.74GB) — al momento di stesura non trovato pubblicamente convertito
- Convertire manualmente con `rknn-llm` toolchain (richiede x86 GPU host)

## Riferimenti rapidi
- Backup pre-pgvector: `/opt/cara/backups/cara-pre-pgvector-20260512-085249.dump`
- Backup .env pre-3B: `/opt/cara/.env.bak-20260512-105334`
- Memoria sessione Nyx: `/home/apedo/.openclaw/workspace/memory/2026-05-12.md`
