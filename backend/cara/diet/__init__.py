"""CARA Nutrizione — diet plan data + seed.

`catalog` holds the structured transcription of the dietista PDFs
(Dott.ssa Elena Poletti). `seed` parses the source PDFs for traceability
and upserts the catalog into the DB idempotently.
"""
